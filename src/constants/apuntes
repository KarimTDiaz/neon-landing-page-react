componentes para el header:

boton = yo veo dos botones , uno con flecha y otro sin flecha, de los que tienen flecha veo uno diferente , el largo , per se ppodria aprovechar el mismo ;

header = header top: con el logo y dos botones ; header data:  con un titlo, un p y un div con dos botones;

