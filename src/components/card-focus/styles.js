import styled from 'styled-components';

const StyledFocusCard = styled.div`
margin-bottom: 2rem;
`;
const FocusCardIcon = styled.img`
margin-left: auto;
margin-right: auto;
`;

export {
    StyledFocusCard,
    FocusCardIcon
};
